---
import type { PostDateProps } from '@/types/components.types';

const { pubDate } = Astro.props as PostDateProps;
---

<time
  datetime={ pubDate.toISOString() }
  class="opacity-50 text-sm"
>
  { pubDate.toLocaleDateString() }
</time>
